import { projects } from './projects'
import { languageGroups, toolGroups } from './skills'

import { aboutMeHTML, contactHTML, projectCardHTML, skillGroupHTML } from './templates'

import type { Project, SkillGroup } from './types'

export { projects, languageGroups, toolGroups }

export {aboutMeHTML, contactHTML, projectCardHTML, skillGroupHTML }

export type { Project, SkillGroup }
